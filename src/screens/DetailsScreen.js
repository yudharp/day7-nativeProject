import React from 'react';
import { View, Text} from 'react-native';

const DetailScreen = () => {
    return(
        <View style = {{ flex :1, justifyContent: 'center', alignItems : 'cente' }}> 
        <Text>Detail Screen</Text>
        </View>
    );
};

export default DetailsScreen;
